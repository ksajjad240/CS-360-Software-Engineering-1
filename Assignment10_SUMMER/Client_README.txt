cd to the directoy where the code client.js is saved

Run the following commands to initialize the client app:

npm init -y
npm install express body-parser axios

Run the following commands to start the client app:
node client.js
