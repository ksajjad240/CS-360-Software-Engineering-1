cd to the directoy where the code server.js is saved

Run the following commands to initialize the server app:

npm init -y
npm install express body-parser bcrypt jsonwebtoken uuid

Run the following commands to start the server app:
node server.js
